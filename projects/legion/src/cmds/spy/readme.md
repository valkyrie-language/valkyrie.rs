# `legion spy wasm`

`legion spy wasm` 提供 `WASM` 二进制的反汇编与分析能力。

## 支持的能力

- 解析任意 `WASM` 二进制。
- 列出所有段。
- 列出所有函数。
- 反汇编指定函数。
- 查看 `imports` / `exports`。
- 按绝对偏移定位错误上下文。
- 以 `JSON` 形式输出。
- dump 函数体原始字节。
