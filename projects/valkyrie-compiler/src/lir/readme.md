# lir

这里承载目标分区后的 `LIR`。

## 职责
- 接住 artifact 分区后的结果。
- 表达靠近目标的低层操作与控制流形状。
- 为 `CLR` 主线和其他次要目标分别留出分路空间。
- 控制流 lane 承载边界见 `control_flow.next.md`。

## 禁止
- 不把所有目标重新糊成一份共享大 `LIR`。
- 不把尚未闭合的语义拖到这里再补。
- 不让 `LIR` 反向侵入 parser 或 runtime。
